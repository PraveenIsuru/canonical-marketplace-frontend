Route group `(buyer)`. Screens land here per the milestone roadmap in
`development-docs/frontend-build-plan.md`. Route groups map to access level, not to
visual layout, so whether a route belongs behind authentication is visible from the
folder it sits in.
